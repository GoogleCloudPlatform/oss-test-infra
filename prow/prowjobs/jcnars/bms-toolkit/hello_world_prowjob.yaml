presubmits:
  jcnars/bms-toolkit:
  - name: testjob
    cluster: build-bms-oracle-team
    always_run: true
    decorate: true
    spec:
      containers:
      - image: gcr.io/google.com/cloudsdktool/cloud-sdk:alpine
        command:
        - "ls"
        resources:
          requests:
            memory: "2.0Gi"
            cpu: "3.0"
